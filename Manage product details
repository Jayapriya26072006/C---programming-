#include <stdio.h>

struct product {
    int pid;
    char pname[20];
    float price;
};

int main() {
    struct product p;
    scanf("%d %s %f", &p.pid, p.pname, &p.price);
    printf("Product ID: %d\nName: %s\nPrice: %.2f", p.pid, p.pname, p.price);
    return 0;
}
