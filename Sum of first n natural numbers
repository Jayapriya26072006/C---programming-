#include <stdio.h>

int main()
{
    int n;
    int i;
    int sum = 0;

    printf("Enter n value: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        sum = sum + i;
    }

    printf("Sum = %d\n", sum);

    return 0;
}
