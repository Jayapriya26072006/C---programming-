#include <stdio.h>

struct patient {
    int pid;
    char name[20];
    int age;
};

int main() {
    struct patient p;
    scanf("%d %s %d", &p.pid, p.name, &p.age);
    printf("ID: %d\nName: %s\nAge: %d", p.pid, p.name, p.age);
    return 0;
}
