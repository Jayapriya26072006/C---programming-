#include <stdio.h>

int main()
{
    int a[10];
    int i;
    int count = 0;

    for (i = 0; i < 10; i++)
    {
        scanf("%d", &a[i]);
        if (a[i] == 1)
            count++;
    }

    printf("Present students = %d\n", count);
    return 0;
}
