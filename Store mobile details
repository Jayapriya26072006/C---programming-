#include <stdio.h>

struct mobile {
    char brand[20];
    char model[20];
    float price;
};

int main() {
    struct mobile m;
    scanf("%s %s %f", m.brand, m.model, &m.price);
    printf("Brand: %s\nModel: %s\nPrice: %.2f", m.brand, m.model, m.price);
    return 0;
}
