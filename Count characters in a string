#include <stdio.h>

int main()
{
    char s[50];
    int i = 0;
    int count = 0;

    gets(s);

    while (s[i] != '\0')
    {
        count++;
        i++;
    }

    printf("Characters = %d\n", count);
    return 0;
}
