#include <stdio.h>

int main()
{
    char s[50];
    int i = 0;
    int count = 0;

    fgets(s, sizeof(s), stdin);

    while (s[i] != '\0' && s[i] != '\n')
    {
        count++;
        i++;
    }

    printf("Characters = %d\n", count);
    return 0;
}
