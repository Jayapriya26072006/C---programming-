#include <stdio.h>
#include <string.h>

int main()
{
    char s[50];
    int i;
    int len;

    fgets(s, sizeof(s), stdin);
    len = strlen(s);

    if (s[len - 1] == '\n')
        len--;

    for (i = len - 1; i >= 0; i--)
        printf("%c", s[i]);

    return 0;
}
