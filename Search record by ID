#include <stdio.h>

struct emp {
    int id;
    char name[20];
};

int main() {
    struct emp e[3];
    int i, sid;

    for (i = 0; i < 3; i++)
        scanf("%d %s", &e[i].id, e[i].name);

    scanf("%d", &sid);

    for (i = 0; i < 3; i++) {
        if (e[i].id == sid) {
            printf("%d %s", e[i].id, e[i].name);
            return 0;
        }
    }
    printf("Not Found");
    return 0;
}
