#include <stdio.h>
#include <ctype.h>

int main()
{
    char s[50];
    int i = 0;

    fgets(s, sizeof(s), stdin);

    while (s[i] != '\0')
    {
        s[i] = toupper(s[i]);
        i++;
    }

    printf("%s", s);
    return 0;
}
