#include <stdio.h>
#include <ctype.h>

int main()
{
    char s[50];
    int i = 0;

    gets(s);

    while (s[i] != '\0')
    {
        s[i] = toupper(s[i]);
        i++;
    }

    printf("%s\n", s);
    return 0;
}
