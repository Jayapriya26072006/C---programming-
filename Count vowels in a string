#include <stdio.h>

int main()
{
    char s[50];
    int i = 0;
    int count = 0;

    gets(s);

    while (s[i] != '\0')
    {
        if (s[i]=='a'||s[i]=='e'||s[i]=='i'||s[i]=='o'||s[i]=='u')
            count++;
        i++;
    }

    printf("Vowels = %d\n", count);
    return 0;
}
