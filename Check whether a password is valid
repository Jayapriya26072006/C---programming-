#include <stdio.h>
#include <string.h>

int main()
{
    char pass[20];
    int len;

    scanf("%s", pass);
    len = strlen(pass);

    if (len >= 6)
        printf("Valid password\n");
    else
        printf("Invalid password\n");

    return 0;
}
