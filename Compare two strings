#include <stdio.h>
#include <string.h>

int main()
{
    char a[20];
    char b[20];

    gets(a);
    gets(b);

    if (strcmp(a, b) == 0)
        printf("Strings are equal\n");
    else
        printf("Strings are not equal\n");

    return 0;
}
