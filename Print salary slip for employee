#include <stdio.h>

int main()
{
    float basic;
    float hra;
    float da;
    float total;

    printf("Enter basic salary: ");
    scanf("%f", &basic);

    hra = basic * 0.20;
    da = basic * 0.10;

    total = basic + hra + da;

    printf("Total salary = %.2f\n", total);

    return 0;
}
