#include <stdio.h>

int main()
{
    int units;
    float bill;
    float rate;

    rate = 5.0;

    printf("Enter units consumed: ");
    scanf("%d", &units);

    bill = units * rate;

    printf("Electricity bill amount = %.2f\n", bill);

    return 0;
}
