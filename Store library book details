#include <stdio.h>

struct book {
    int id;
    char title[30];
    float price;
};

int main() {
    struct book b;
    scanf("%d %s %f", &b.id, b.title, &b.price);
    printf("Book ID: %d\nTitle: %s\nPrice: %.2f", b.id, b.title, b.price);
    return 0;
}
