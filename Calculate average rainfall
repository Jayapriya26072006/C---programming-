#include <stdio.h>

int main()
{
    float rain[7];
    float sum = 0;
    float avg;
    int i;

    for (i = 0; i < 7; i++)
    {
        scanf("%f", &rain[i]);
        sum = sum + rain[i];
    }

    avg = sum / 7;
    printf("Average rainfall = %.2f\n", avg);

    return 0;
}
