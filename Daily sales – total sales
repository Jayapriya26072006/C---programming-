#include <stdio.h>

int main() {
    int n, i;
    float sale, total = 0;

    scanf("%d", &n);

    for (i = 0; i < n; i++) {
        scanf("%f", &sale);
        total += sale;
    }

    printf("Total Sales = %.2f", total);
    return 0;
}
