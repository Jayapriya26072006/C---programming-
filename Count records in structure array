#include <stdio.h>

struct student {
    int id;
    char name[20];
};

int main() {
    struct student s[5];
    int n, i;

    scanf("%d", &n);

    for (i = 0; i < n; i++)
        scanf("%d %s", &s[i].id, s[i].name);

    printf("Total Records = %d", n);
    return 0;
}
