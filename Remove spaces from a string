#include <stdio.h>

int main()
{
    char s[50];
    int i, j = 0;

    fgets(s, sizeof(s), stdin);

    for (i = 0; s[i] != '\0'; i++)
    {
        if (s[i] != ' ' && s[i] != '\n')
            s[j++] = s[i];
    }

    s[j] = '\0';
    printf("%s\n", s);

    return 0;
}
