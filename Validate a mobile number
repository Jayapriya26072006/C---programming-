#include <stdio.h>
#include <string.h>

int main()
{
    char m[15];
    int len;

    gets(m);
    len = strlen(m);

    if (len == 10)
        printf("Valid mobile number\n");
    else
        printf("Invalid mobile number\n");

    return 0;
}
